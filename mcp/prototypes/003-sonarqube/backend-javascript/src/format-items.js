function formatPersons(persons) {
  return persons.map(person => ({
    ...person,
    firstName: person.name,
    status: 'active',
  }));
}
function formatPersons1(persons) {
  return persons.map(person => ({
    ...person,
    firstName: person.name,
    status: 'active',
  }));
}
function formatPersons2(persons) {
  return persons.map(person => ({
    ...person,
    firstName: person.name,
    status: 'active',
  }));
}
function formatPersons3(persons) {
  return persons.map(person => ({
    ...person,
    firstName: person.name,
    status: 'active',
  }));
}

export default formatPersons;
