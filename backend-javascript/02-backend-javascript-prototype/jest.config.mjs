// const config = {
//   coverageProvider: 'v8',
// };

// export default config;


const config = {
  testEnvironment: 'node',
  coverageProvider: 'v8',
  extensionsToTreatAsEsm: ['.js'],
  transform: {},
};

export default config;