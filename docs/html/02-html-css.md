# Type de sélecteurs

	selecteur de type
    p { color: blue; }
    h1 { font-size: 2em; }

	selecteur de classe
    .bouton { background: red; }
    .titre-principal { font-weight: bold; }

	selecteur d'ID
    #header { height: 100px; }

